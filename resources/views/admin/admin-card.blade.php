<div class="card">
    <div class="card-body">
      <h5 class="card-title"><i class="{{$icon ?? "fa-solid fa-users"}}"></i>{{$title ?? ''}}</h5>
      <p class="card-text fs-2">{{$data ?? 0}}</p> 
    </div>
  </div>